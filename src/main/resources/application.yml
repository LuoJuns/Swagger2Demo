spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/security
    username: root
    password: root

mybatis-plus:
  mapper-locations: classpath*:com/swagger2demo/mapper/*Mapper.xml
